
use "../GLib"
use "../Gio"
use "../GObject"
//  use "../Gdk"
//  use "../Gsk"
use "lib:gtk-4"

primitive GtkRevealerSys
// Static Functions

// Constructors
/* 
fun create(): Pointer[GtkWidgetT] =>
    @gtk_revealer_new()
*/

// Methods
/* 
fun gtk_revealer_get_child(revealer': NullablePointer[GtkRevealerT]): Pointer[GtkWidgetT] =>
    @gtk_revealer_get_child(revealer')
*/
/* 
fun gtk_revealer_get_child_revealed(revealer': NullablePointer[GtkRevealerT]): I32 =>
    @gtk_revealer_get_child_revealed(revealer')
*/
/* 
fun gtk_revealer_get_reveal_child(revealer': NullablePointer[GtkRevealerT]): I32 =>
    @gtk_revealer_get_reveal_child(revealer')
*/
/* 
fun gtk_revealer_get_transition_duration(revealer': NullablePointer[GtkRevealerT]): U32 =>
    @gtk_revealer_get_transition_duration(revealer')
*/
/* 
fun gtk_revealer_get_transition_type(revealer': NullablePointer[GtkRevealerT]): U32 =>
    @gtk_revealer_get_transition_type(revealer')
*/
/* 
fun gtk_revealer_set_child(revealer': NullablePointer[GtkRevealerT], child': NullablePointer[GtkWidgetT]): None =>
    @gtk_revealer_set_child(revealer', child')
*/
/* 
fun gtk_revealer_set_reveal_child(revealer': NullablePointer[GtkRevealerT], reveal_child': I32): None =>
    @gtk_revealer_set_reveal_child(revealer', reveal_child')
*/
/* 
fun gtk_revealer_set_transition_duration(revealer': NullablePointer[GtkRevealerT], duration': U32): None =>
    @gtk_revealer_set_transition_duration(revealer', duration')
*/
/* 
fun gtk_revealer_set_transition_type(revealer': NullablePointer[GtkRevealerT], transition': U32): None =>
    @gtk_revealer_set_transition_type(revealer', transition')
*/
