
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GAppLaunchContextClass
  var ptr: NullablePointer[GAppLaunchContextClassT]

  new create(ptr': NullablePointer[GAppLaunchContextClassT]) => ptr = ptr'
