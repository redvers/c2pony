
use "../GLib"
use "../GObject"
use "lib:gio-2.0"
struct GFileMonitorT
  embed parent_instance': GObjectT = GObjectT
  var priv': NullablePointer[GFileMonitorPrivateT] = NullablePointer[GFileMonitorPrivateT].none()
