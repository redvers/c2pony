
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GProxyAddressEnumeratorPrivate
  var ptr: NullablePointer[GProxyAddressEnumeratorPrivateT]

  new create(ptr': NullablePointer[GProxyAddressEnumeratorPrivateT]) => ptr = ptr'
