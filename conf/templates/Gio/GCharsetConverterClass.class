
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GCharsetConverterClass
  var ptr: NullablePointer[GCharsetConverterClassT]

  new create(ptr': NullablePointer[GCharsetConverterClassT]) => ptr = ptr'
