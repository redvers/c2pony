
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileIOStreamPrivate
  var ptr: NullablePointer[GFileIOStreamPrivateT]

  new create_from_ptr(ptr': NullablePointer[GFileIOStreamPrivateT]) => ptr = ptr'
