
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileIOStreamPrivate
  var ptr: NullablePointer[GFileIOStreamPrivateT]

  new create(ptr': NullablePointer[GFileIOStreamPrivateT]) => ptr = ptr'
