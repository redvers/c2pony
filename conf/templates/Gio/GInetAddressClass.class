
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GInetAddressClass
  var ptr: NullablePointer[GInetAddressClassT]

  new create() =>
    ptr = NullablePointer[GInetAddressClassT].none()
  new create_from_ptr(ptr': NullablePointer[GInetAddressClassT]) => ptr = ptr'
