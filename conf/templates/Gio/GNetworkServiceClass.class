
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GNetworkServiceClass
  var ptr: NullablePointer[GNetworkServiceClassT]

  new create_from_ptr(ptr': NullablePointer[GNetworkServiceClassT]) => ptr = ptr'
