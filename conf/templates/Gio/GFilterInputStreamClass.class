
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFilterInputStreamClass
  var ptr: NullablePointer[GFilterInputStreamClassT]

  new create(ptr': NullablePointer[GFilterInputStreamClassT]) => ptr = ptr'
