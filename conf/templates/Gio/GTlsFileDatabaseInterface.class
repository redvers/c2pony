
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsFileDatabaseInterface
  var ptr: NullablePointer[GTlsFileDatabaseInterfaceT]

  new create() =>
    ptr = NullablePointer[GTlsFileDatabaseInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GTlsFileDatabaseInterfaceT]) => ptr = ptr'
