
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GApplicationClass
  var ptr: NullablePointer[GApplicationClassT]

  new create() =>
    ptr = NullablePointer[GApplicationClassT].none()
  new create_from_ptr(ptr': NullablePointer[GApplicationClassT]) => ptr = ptr'
