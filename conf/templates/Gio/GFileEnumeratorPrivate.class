
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileEnumeratorPrivate
  var ptr: NullablePointer[GFileEnumeratorPrivateT]

  new create(ptr': NullablePointer[GFileEnumeratorPrivateT]) => ptr = ptr'
