
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileEnumeratorPrivate
  var ptr: NullablePointer[GFileEnumeratorPrivateT]

  new create_from_ptr(ptr': NullablePointer[GFileEnumeratorPrivateT]) => ptr = ptr'
