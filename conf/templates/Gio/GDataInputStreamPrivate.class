
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDataInputStreamPrivate
  var ptr: NullablePointer[GDataInputStreamPrivateT]

  new create(ptr': NullablePointer[GDataInputStreamPrivateT]) => ptr = ptr'
