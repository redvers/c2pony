
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GProxyAddressClass
  var ptr: NullablePointer[GProxyAddressClassT]

  new create_from_ptr(ptr': NullablePointer[GProxyAddressClassT]) => ptr = ptr'
