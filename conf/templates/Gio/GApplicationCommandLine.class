
use @g_application_command_line_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GApplicationCommandLine is GApplicationCommandLineI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_application_command_line_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GApplicationCommandLine")
    GObjectP.set_data_p[GApplicationCommandLine](ptr, "_PonyGObjectPonyObject_", this)

  // GApplicationCommandLine
  fun ref getptr(): NullablePointer[GObjectT] => ptr
