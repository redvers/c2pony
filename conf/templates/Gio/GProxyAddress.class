
use @g_proxy_address_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GProxyAddress is GProxyAddressI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_proxy_address_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GProxyAddress")
    GObjectP.set_data_p[GProxyAddress](ptr, "_PonyGObjectPonyObject_", this)

  // GProxyAddress
  fun ref getptr(): NullablePointer[GObjectT] => ptr
