
use @g_tls_database_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsDatabase is GTlsDatabaseI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_tls_database_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GTlsDatabase")
    GObjectP.set_data_p[GTlsDatabase](ptr, "_PonyGObjectPonyObject_", this)

  // GTlsDatabase
  fun ref getptr(): NullablePointer[GObjectT] => ptr
