
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDtlsConnectionInterface
  var ptr: NullablePointer[GDtlsConnectionInterfaceT]

  new create(ptr': NullablePointer[GDtlsConnectionInterfaceT]) => ptr = ptr'
