
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GListModelInterface
  var ptr: NullablePointer[GListModelInterfaceT]

  new create() =>
    ptr = NullablePointer[GListModelInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GListModelInterfaceT]) => ptr = ptr'
