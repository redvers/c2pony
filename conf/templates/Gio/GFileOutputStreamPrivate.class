
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileOutputStreamPrivate
  var ptr: NullablePointer[GFileOutputStreamPrivateT]

  new create(ptr': NullablePointer[GFileOutputStreamPrivateT]) => ptr = ptr'
