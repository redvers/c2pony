
use @g_unix_output_stream_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixOutputStream is GUnixOutputStreamI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_unix_output_stream_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GUnixOutputStream")
    GObjectP.set_data_p[GUnixOutputStream](ptr, "_PonyGObjectPonyObject_", this)

  // GUnixOutputStream
  fun ref getptr(): NullablePointer[GObjectT] => ptr
