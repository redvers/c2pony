
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketPrivate
  var ptr: NullablePointer[GSocketPrivateT]

  new create() =>
    ptr = NullablePointer[GSocketPrivateT].none()
  new create_from_ptr(ptr': NullablePointer[GSocketPrivateT]) => ptr = ptr'
