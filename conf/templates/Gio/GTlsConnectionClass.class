
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsConnectionClass
  var ptr: NullablePointer[GTlsConnectionClassT]

  new create() =>
    ptr = NullablePointer[GTlsConnectionClassT].none()
  new create_from_ptr(ptr': NullablePointer[GTlsConnectionClassT]) => ptr = ptr'
