
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsClientConnectionInterface
  var ptr: NullablePointer[GTlsClientConnectionInterfaceT]

  new create() =>
    ptr = NullablePointer[GTlsClientConnectionInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GTlsClientConnectionInterfaceT]) => ptr = ptr'
