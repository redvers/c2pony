
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSimpleProxyResolverPrivate
  var ptr: NullablePointer[GSimpleProxyResolverPrivateT]

  new create(ptr': NullablePointer[GSimpleProxyResolverPrivateT]) => ptr = ptr'
