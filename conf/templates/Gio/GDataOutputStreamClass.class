
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDataOutputStreamClass
  var ptr: NullablePointer[GDataOutputStreamClassT]

  new create(ptr': NullablePointer[GDataOutputStreamClassT]) => ptr = ptr'
