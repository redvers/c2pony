
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsConnectionPrivate
  var ptr: NullablePointer[GTlsConnectionPrivateT]

  new create_from_ptr(ptr': NullablePointer[GTlsConnectionPrivateT]) => ptr = ptr'
