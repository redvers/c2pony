
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileMonitorClass
  var ptr: NullablePointer[GFileMonitorClassT]

  new create_from_ptr(ptr': NullablePointer[GFileMonitorClassT]) => ptr = ptr'
