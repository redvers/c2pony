
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GIOExtension
  var ptr: NullablePointer[GIOExtensionT]

  new create() =>
    ptr = NullablePointer[GIOExtensionT].none()
  new create_from_ptr(ptr': NullablePointer[GIOExtensionT]) => ptr = ptr'
