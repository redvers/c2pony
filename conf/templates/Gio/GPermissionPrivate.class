
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GPermissionPrivate
  var ptr: NullablePointer[GPermissionPrivateT]

  new create() =>
    ptr = NullablePointer[GPermissionPrivateT].none()
  new create_from_ptr(ptr': NullablePointer[GPermissionPrivateT]) => ptr = ptr'
