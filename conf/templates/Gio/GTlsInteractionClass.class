
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsInteractionClass
  var ptr: NullablePointer[GTlsInteractionClassT]

  new create(ptr': NullablePointer[GTlsInteractionClassT]) => ptr = ptr'
