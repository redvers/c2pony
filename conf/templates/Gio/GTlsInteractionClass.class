
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsInteractionClass
  var ptr: NullablePointer[GTlsInteractionClassT]

  new create() =>
    ptr = NullablePointer[GTlsInteractionClassT].none()
  new create_from_ptr(ptr': NullablePointer[GTlsInteractionClassT]) => ptr = ptr'
