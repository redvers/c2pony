
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFilterOutputStreamClass
  var ptr: NullablePointer[GFilterOutputStreamClassT]

  new create() =>
    ptr = NullablePointer[GFilterOutputStreamClassT].none()
  new create_from_ptr(ptr': NullablePointer[GFilterOutputStreamClassT]) => ptr = ptr'
