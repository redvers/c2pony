
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketClientClass
  var ptr: NullablePointer[GSocketClientClassT]

  new create() =>
    ptr = NullablePointer[GSocketClientClassT].none()
  new create_from_ptr(ptr': NullablePointer[GSocketClientClassT]) => ptr = ptr'
