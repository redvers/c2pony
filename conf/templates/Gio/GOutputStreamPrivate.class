
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GOutputStreamPrivate
  var ptr: NullablePointer[GOutputStreamPrivateT]

  new create(ptr': NullablePointer[GOutputStreamPrivateT]) => ptr = ptr'
