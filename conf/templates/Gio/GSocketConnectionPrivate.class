
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketConnectionPrivate
  var ptr: NullablePointer[GSocketConnectionPrivateT]

  new create_from_ptr(ptr': NullablePointer[GSocketConnectionPrivateT]) => ptr = ptr'
