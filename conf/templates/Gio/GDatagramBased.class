

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDatagramBased is GDatagramBasedI
  var ptr: NullablePointer[T]

  new create_from_ptr(ptr': NullablePointer[T]) => ptr = ptr'


  // GDatagramBased
  fun ref getptr(): NullablePointer[T] => ptr
