
use "../GLib"
use "../GObject"
use "lib:gio-2.0"
struct GTlsConnectionT
  embed parent_instance': GIOStreamT = GIOStreamT
  var priv': NullablePointer[GTlsConnectionPrivateT] = NullablePointer[GTlsConnectionPrivateT].none()
