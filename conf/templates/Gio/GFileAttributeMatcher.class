
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileAttributeMatcher
  var ptr: NullablePointer[GFileAttributeMatcherT]

  new create(ptr': NullablePointer[GFileAttributeMatcherT]) => ptr = ptr'
