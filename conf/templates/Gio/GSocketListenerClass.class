
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketListenerClass
  var ptr: NullablePointer[GSocketListenerClassT]

  new create(ptr': NullablePointer[GSocketListenerClassT]) => ptr = ptr'
