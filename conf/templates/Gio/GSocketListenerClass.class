
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketListenerClass
  var ptr: NullablePointer[GSocketListenerClassT]

  new create_from_ptr(ptr': NullablePointer[GSocketListenerClassT]) => ptr = ptr'
