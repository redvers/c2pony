
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileIOStreamClass
  var ptr: NullablePointer[GFileIOStreamClassT]

  new create() =>
    ptr = NullablePointer[GFileIOStreamClassT].none()
  new create_from_ptr(ptr': NullablePointer[GFileIOStreamClassT]) => ptr = ptr'
