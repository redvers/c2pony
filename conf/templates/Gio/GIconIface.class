
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GIconIface
  var ptr: NullablePointer[GIconIfaceT]

  new create() =>
    ptr = NullablePointer[GIconIfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GIconIfaceT]) => ptr = ptr'
