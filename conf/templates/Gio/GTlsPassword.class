
use @g_tls_password_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsPassword is GTlsPasswordI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_tls_password_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GTlsPassword")
    GObjectP.set_data_p[GTlsPassword](ptr, "_PonyGObjectPonyObject_", this)

  // GTlsPassword
  fun ref getptr(): NullablePointer[GObjectT] => ptr
