
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketServicePrivate
  var ptr: NullablePointer[GSocketServicePrivateT]

  new create(ptr': NullablePointer[GSocketServicePrivateT]) => ptr = ptr'
