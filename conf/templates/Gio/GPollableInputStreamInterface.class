
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GPollableInputStreamInterface
  var ptr: NullablePointer[GPollableInputStreamInterfaceT]

  new create() =>
    ptr = NullablePointer[GPollableInputStreamInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GPollableInputStreamInterfaceT]) => ptr = ptr'
