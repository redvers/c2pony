
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketClientPrivate
  var ptr: NullablePointer[GSocketClientPrivateT]

  new create(ptr': NullablePointer[GSocketClientPrivateT]) => ptr = ptr'
