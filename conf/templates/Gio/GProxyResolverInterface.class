
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GProxyResolverInterface
  var ptr: NullablePointer[GProxyResolverInterfaceT]

  new create(ptr': NullablePointer[GProxyResolverInterfaceT]) => ptr = ptr'
