
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GLoadableIconIface
  var ptr: NullablePointer[GLoadableIconIfaceT]

  new create_from_ptr(ptr': NullablePointer[GLoadableIconIfaceT]) => ptr = ptr'
