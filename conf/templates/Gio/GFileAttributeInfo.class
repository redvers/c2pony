
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileAttributeInfo
  var ptr: NullablePointer[GFileAttributeInfoT]

  new create() =>
    ptr = NullablePointer[GFileAttributeInfoT].none()
  new create_from_ptr(ptr': NullablePointer[GFileAttributeInfoT]) => ptr = ptr'
