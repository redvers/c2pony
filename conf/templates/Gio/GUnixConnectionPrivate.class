
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixConnectionPrivate
  var ptr: NullablePointer[GUnixConnectionPrivateT]

  new create(ptr': NullablePointer[GUnixConnectionPrivateT]) => ptr = ptr'
