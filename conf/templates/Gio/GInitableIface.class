
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GInitableIface
  var ptr: NullablePointer[GInitableIfaceT]

  new create() =>
    ptr = NullablePointer[GInitableIfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GInitableIfaceT]) => ptr = ptr'
