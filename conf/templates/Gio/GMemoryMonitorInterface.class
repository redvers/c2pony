
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryMonitorInterface
  var ptr: NullablePointer[GMemoryMonitorInterfaceT]

  new create_from_ptr(ptr': NullablePointer[GMemoryMonitorInterfaceT]) => ptr = ptr'
