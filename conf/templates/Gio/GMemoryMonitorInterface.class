
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryMonitorInterface
  var ptr: NullablePointer[GMemoryMonitorInterfaceT]

  new create(ptr': NullablePointer[GMemoryMonitorInterfaceT]) => ptr = ptr'
