
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDriveIface
  var ptr: NullablePointer[GDriveIfaceT]

  new create() =>
    ptr = NullablePointer[GDriveIfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GDriveIfaceT]) => ptr = ptr'
