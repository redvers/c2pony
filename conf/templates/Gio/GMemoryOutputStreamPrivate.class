
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryOutputStreamPrivate
  var ptr: NullablePointer[GMemoryOutputStreamPrivateT]

  new create_from_ptr(ptr': NullablePointer[GMemoryOutputStreamPrivateT]) => ptr = ptr'
