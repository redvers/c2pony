
use @g_desktop_app_info_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDesktopAppInfo is GDesktopAppInfoI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_desktop_app_info_get_type())

  // GDesktopAppInfo
  fun ref getptr(): NullablePointer[GObjectT] => ptr
