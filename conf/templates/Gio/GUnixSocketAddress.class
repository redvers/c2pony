
use @g_unix_socket_address_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixSocketAddress is GUnixSocketAddressI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_unix_socket_address_get_type())
    GObjectP.set_data_p[String](ptr, "_PonyGObjectTypeString_", "GUnixSocketAddress")
    GObjectP.set_data_p[GUnixSocketAddress](ptr, "_PonyGObjectPonyObject_", this)

  // GUnixSocketAddress
  fun ref getptr(): NullablePointer[GObjectT] => ptr
