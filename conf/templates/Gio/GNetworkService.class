
use @g_network_service_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GNetworkService is GNetworkServiceI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_network_service_get_type())

  // GNetworkService
  fun ref getptr(): NullablePointer[GObjectT] => ptr
