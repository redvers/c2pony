
use @g_zlib_decompressor_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GZlibDecompressor is GZlibDecompressorI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_zlib_decompressor_get_type())

  // GZlibDecompressor
  fun ref getptr(): NullablePointer[GObjectT] => ptr
