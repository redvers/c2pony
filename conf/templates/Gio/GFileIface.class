
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileIface
  var ptr: NullablePointer[GFileIfaceT]

  new create() =>
    ptr = NullablePointer[GFileIfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GFileIfaceT]) => ptr = ptr'
