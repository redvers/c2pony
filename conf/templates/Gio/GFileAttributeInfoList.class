
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileAttributeInfoList
  var ptr: NullablePointer[GFileAttributeInfoListT]

  new create() =>
    ptr = NullablePointer[GFileAttributeInfoListT].none()
  new create_from_ptr(ptr': NullablePointer[GFileAttributeInfoListT]) => ptr = ptr'
