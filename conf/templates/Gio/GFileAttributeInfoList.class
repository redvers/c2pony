
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileAttributeInfoList
  var ptr: NullablePointer[GFileAttributeInfoListT]

  new create(ptr': NullablePointer[GFileAttributeInfoListT]) => ptr = ptr'
