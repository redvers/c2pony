
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryOutputStreamClass
  var ptr: NullablePointer[GMemoryOutputStreamClassT]

  new create_from_ptr(ptr': NullablePointer[GMemoryOutputStreamClassT]) => ptr = ptr'
