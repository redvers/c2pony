
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketConnectionClass
  var ptr: NullablePointer[GSocketConnectionClassT]

  new create(ptr': NullablePointer[GSocketConnectionClassT]) => ptr = ptr'
