
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GFileInfoClass
  var ptr: NullablePointer[GFileInfoClassT]

  new create() =>
    ptr = NullablePointer[GFileInfoClassT].none()
  new create_from_ptr(ptr': NullablePointer[GFileInfoClassT]) => ptr = ptr'
