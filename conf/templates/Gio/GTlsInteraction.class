
use @g_tls_interaction_get_type[U64]()

use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsInteraction is GTlsInteractionI
  var ptr: NullablePointer[GObjectT]

  new create_from_ptr(ptr': NullablePointer[GObjectT]) => ptr = ptr'

  new create() =>
    ptr = GObjectG.gnew(@g_tls_interaction_get_type())

  // GTlsInteraction
  fun ref getptr(): NullablePointer[GObjectT] => ptr
