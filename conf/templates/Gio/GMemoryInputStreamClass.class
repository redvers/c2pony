
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryInputStreamClass
  var ptr: NullablePointer[GMemoryInputStreamClassT]

  new create(ptr': NullablePointer[GMemoryInputStreamClassT]) => ptr = ptr'
