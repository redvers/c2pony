
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryInputStreamClass
  var ptr: NullablePointer[GMemoryInputStreamClassT]

  new create_from_ptr(ptr': NullablePointer[GMemoryInputStreamClassT]) => ptr = ptr'
