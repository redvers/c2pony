
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GCancellableClass
  var ptr: NullablePointer[GCancellableClassT]

  new create() =>
    ptr = NullablePointer[GCancellableClassT].none()
  new create_from_ptr(ptr': NullablePointer[GCancellableClassT]) => ptr = ptr'
