
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixConnectionClass
  var ptr: NullablePointer[GUnixConnectionClassT]

  new create(ptr': NullablePointer[GUnixConnectionClassT]) => ptr = ptr'
