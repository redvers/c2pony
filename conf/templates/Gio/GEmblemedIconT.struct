
use "../GLib"
use "../GObject"
use "lib:gio-2.0"
struct GEmblemedIconT
  embed parent_instance': GObjectT = GObjectT
  var priv': NullablePointer[GEmblemedIconPrivateT] = NullablePointer[GEmblemedIconPrivateT].none()
