
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GTlsPasswordClass
  var ptr: NullablePointer[GTlsPasswordClassT]

  new create() =>
    ptr = NullablePointer[GTlsPasswordClassT].none()
  new create_from_ptr(ptr': NullablePointer[GTlsPasswordClassT]) => ptr = ptr'
