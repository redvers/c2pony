
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GEmblemedIconPrivate
  var ptr: NullablePointer[GEmblemedIconPrivateT]

  new create(ptr': NullablePointer[GEmblemedIconPrivateT]) => ptr = ptr'
