
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GMemoryInputStreamPrivate
  var ptr: NullablePointer[GMemoryInputStreamPrivateT]

  new create_from_ptr(ptr': NullablePointer[GMemoryInputStreamPrivateT]) => ptr = ptr'
