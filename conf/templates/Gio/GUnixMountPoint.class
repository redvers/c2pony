
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixMountPoint
  var ptr: NullablePointer[GUnixMountPointT]

  new create() =>
    ptr = NullablePointer[GUnixMountPointT].none()
  new create_from_ptr(ptr': NullablePointer[GUnixMountPointT]) => ptr = ptr'
