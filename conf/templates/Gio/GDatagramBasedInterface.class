
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDatagramBasedInterface
  var ptr: NullablePointer[GDatagramBasedInterfaceT]

  new create() =>
    ptr = NullablePointer[GDatagramBasedInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GDatagramBasedInterfaceT]) => ptr = ptr'
