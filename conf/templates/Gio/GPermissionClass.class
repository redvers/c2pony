
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GPermissionClass
  var ptr: NullablePointer[GPermissionClassT]

  new create() =>
    ptr = NullablePointer[GPermissionClassT].none()
  new create_from_ptr(ptr': NullablePointer[GPermissionClassT]) => ptr = ptr'
