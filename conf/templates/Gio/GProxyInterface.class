
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GProxyInterface
  var ptr: NullablePointer[GProxyInterfaceT]

  new create() =>
    ptr = NullablePointer[GProxyInterfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GProxyInterfaceT]) => ptr = ptr'
