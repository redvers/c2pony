
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GUnixFDMessageClass
  var ptr: NullablePointer[GUnixFDMessageClassT]

  new create_from_ptr(ptr': NullablePointer[GUnixFDMessageClassT]) => ptr = ptr'
