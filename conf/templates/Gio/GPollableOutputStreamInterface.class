
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GPollableOutputStreamInterface
  var ptr: NullablePointer[GPollableOutputStreamInterfaceT]

  new create_from_ptr(ptr': NullablePointer[GPollableOutputStreamInterfaceT]) => ptr = ptr'
