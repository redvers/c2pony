
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GSocketClass
  var ptr: NullablePointer[GSocketClassT]

  new create() =>
    ptr = NullablePointer[GSocketClassT].none()
  new create_from_ptr(ptr': NullablePointer[GSocketClassT]) => ptr = ptr'
