
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GConverterInputStreamClass
  var ptr: NullablePointer[GConverterInputStreamClassT]

  new create_from_ptr(ptr': NullablePointer[GConverterInputStreamClassT]) => ptr = ptr'
