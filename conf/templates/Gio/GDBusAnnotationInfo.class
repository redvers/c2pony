
use "../GLib"
use "../GObject"
use "lib:gio-2.0"

class GDBusAnnotationInfo
  var ptr: NullablePointer[GDBusAnnotationInfoT]

  new create_from_ptr(ptr': NullablePointer[GDBusAnnotationInfoT]) => ptr = ptr'
