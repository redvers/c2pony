
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkEditableInterface
  var ptr: NullablePointer[GtkEditableInterfaceT]

  new create(ptr': NullablePointer[GtkEditableInterfaceT]) => ptr = ptr'
