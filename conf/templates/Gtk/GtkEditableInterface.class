
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkEditableInterface
  var ptr: NullablePointer[GtkEditableInterfaceT]

  new create_from_ptr(ptr': NullablePointer[GtkEditableInterfaceT]) => ptr = ptr'
