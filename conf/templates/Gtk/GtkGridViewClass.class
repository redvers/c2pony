
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkGridViewClass
  var ptr: NullablePointer[GtkGridViewClassT]

  new create_from_ptr(ptr': NullablePointer[GtkGridViewClassT]) => ptr = ptr'
