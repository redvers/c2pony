
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkConstraintClass
  var ptr: NullablePointer[GtkConstraintClassT]

  new create(ptr': NullablePointer[GtkConstraintClassT]) => ptr = ptr'
