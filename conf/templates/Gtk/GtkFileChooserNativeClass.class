
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkFileChooserNativeClass
  var ptr: NullablePointer[GtkFileChooserNativeClassT]

  new create() =>
    ptr = NullablePointer[GtkFileChooserNativeClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkFileChooserNativeClassT]) => ptr = ptr'
