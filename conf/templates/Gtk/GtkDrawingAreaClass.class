
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkDrawingAreaClass
  var ptr: NullablePointer[GtkDrawingAreaClassT]

  new create(ptr': NullablePointer[GtkDrawingAreaClassT]) => ptr = ptr'
