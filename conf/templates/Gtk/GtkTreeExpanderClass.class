
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeExpanderClass
  var ptr: NullablePointer[GtkTreeExpanderClassT]

  new create() =>
    ptr = NullablePointer[GtkTreeExpanderClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkTreeExpanderClassT]) => ptr = ptr'
