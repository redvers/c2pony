
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkApplicationWindowClass
  var ptr: NullablePointer[GtkApplicationWindowClassT]

  new create(ptr': NullablePointer[GtkApplicationWindowClassT]) => ptr = ptr'
