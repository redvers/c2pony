
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkConstraintLayoutClass
  var ptr: NullablePointer[GtkConstraintLayoutClassT]

  new create(ptr': NullablePointer[GtkConstraintLayoutClassT]) => ptr = ptr'
