
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkLayoutChildClass
  var ptr: NullablePointer[GtkLayoutChildClassT]

  new create_from_ptr(ptr': NullablePointer[GtkLayoutChildClassT]) => ptr = ptr'
