
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkLayoutChildClass
  var ptr: NullablePointer[GtkLayoutChildClassT]

  new create(ptr': NullablePointer[GtkLayoutChildClassT]) => ptr = ptr'
