
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeDragDestIface
  var ptr: NullablePointer[GtkTreeDragDestIfaceT]

  new create() =>
    ptr = NullablePointer[GtkTreeDragDestIfaceT].none()
  new create_from_ptr(ptr': NullablePointer[GtkTreeDragDestIfaceT]) => ptr = ptr'
