
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeDragDestIface
  var ptr: NullablePointer[GtkTreeDragDestIfaceT]

  new create(ptr': NullablePointer[GtkTreeDragDestIfaceT]) => ptr = ptr'
