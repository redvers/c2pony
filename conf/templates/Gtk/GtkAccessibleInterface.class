
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkAccessibleInterface
  var ptr: NullablePointer[GtkAccessibleInterfaceT]

  new create(ptr': NullablePointer[GtkAccessibleInterfaceT]) => ptr = ptr'
