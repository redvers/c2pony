
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeViewClass
  var ptr: NullablePointer[GtkTreeViewClassT]

  new create(ptr': NullablePointer[GtkTreeViewClassT]) => ptr = ptr'
