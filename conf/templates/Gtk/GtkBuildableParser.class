
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkBuildableParser
  var ptr: NullablePointer[GtkBuildableParserT]

  new create(ptr': NullablePointer[GtkBuildableParserT]) => ptr = ptr'
