
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkOrientableIface
  var ptr: NullablePointer[GtkOrientableIfaceT]

  new create(ptr': NullablePointer[GtkOrientableIfaceT]) => ptr = ptr'
