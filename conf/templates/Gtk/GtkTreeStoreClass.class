
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeStoreClass
  var ptr: NullablePointer[GtkTreeStoreClassT]

  new create() =>
    ptr = NullablePointer[GtkTreeStoreClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkTreeStoreClassT]) => ptr = ptr'
