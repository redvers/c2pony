
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeStoreClass
  var ptr: NullablePointer[GtkTreeStoreClassT]

  new create(ptr': NullablePointer[GtkTreeStoreClassT]) => ptr = ptr'
