
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkExpressionWatch
  var ptr: NullablePointer[GtkExpressionWatchT]

  new create(ptr': NullablePointer[GtkExpressionWatchT]) => ptr = ptr'
