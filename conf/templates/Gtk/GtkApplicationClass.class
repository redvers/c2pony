
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkApplicationClass
  var ptr: NullablePointer[GtkApplicationClassT]

  new create(ptr': NullablePointer[GtkApplicationClassT]) => ptr = ptr'
