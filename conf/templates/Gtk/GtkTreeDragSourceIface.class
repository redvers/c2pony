
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeDragSourceIface
  var ptr: NullablePointer[GtkTreeDragSourceIfaceT]

  new create(ptr': NullablePointer[GtkTreeDragSourceIfaceT]) => ptr = ptr'
