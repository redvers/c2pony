
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkGestureZoomClass
  var ptr: NullablePointer[GtkGestureZoomClassT]

  new create(ptr': NullablePointer[GtkGestureZoomClassT]) => ptr = ptr'
