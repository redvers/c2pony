
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkStringSorterClass
  var ptr: NullablePointer[GtkStringSorterClassT]

  new create(ptr': NullablePointer[GtkStringSorterClassT]) => ptr = ptr'
