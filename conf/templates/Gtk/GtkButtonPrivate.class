
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkButtonPrivate
  var ptr: NullablePointer[GtkButtonPrivateT]

  new create(ptr': NullablePointer[GtkButtonPrivateT]) => ptr = ptr'
