
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkScrollableInterface
  var ptr: NullablePointer[GtkScrollableInterfaceT]

  new create(ptr': NullablePointer[GtkScrollableInterfaceT]) => ptr = ptr'
