
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkEmojiChooserClass
  var ptr: NullablePointer[GtkEmojiChooserClassT]

  new create() =>
    ptr = NullablePointer[GtkEmojiChooserClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkEmojiChooserClassT]) => ptr = ptr'
