
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkEmojiChooserClass
  var ptr: NullablePointer[GtkEmojiChooserClassT]

  new create(ptr': NullablePointer[GtkEmojiChooserClassT]) => ptr = ptr'
