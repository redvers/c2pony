
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkSelectionModelInterface
  var ptr: NullablePointer[GtkSelectionModelInterfaceT]

  new create(ptr': NullablePointer[GtkSelectionModelInterfaceT]) => ptr = ptr'
