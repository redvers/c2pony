
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTextViewClass
  var ptr: NullablePointer[GtkTextViewClassT]

  new create() =>
    ptr = NullablePointer[GtkTextViewClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkTextViewClassT]) => ptr = ptr'
