
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTextViewClass
  var ptr: NullablePointer[GtkTextViewClassT]

  new create(ptr': NullablePointer[GtkTextViewClassT]) => ptr = ptr'
