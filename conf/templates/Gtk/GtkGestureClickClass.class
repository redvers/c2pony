
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkGestureClickClass
  var ptr: NullablePointer[GtkGestureClickClassT]

  new create() =>
    ptr = NullablePointer[GtkGestureClickClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkGestureClickClassT]) => ptr = ptr'
