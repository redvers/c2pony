
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkMultiFilterClass
  var ptr: NullablePointer[GtkMultiFilterClassT]

  new create_from_ptr(ptr': NullablePointer[GtkMultiFilterClassT]) => ptr = ptr'
