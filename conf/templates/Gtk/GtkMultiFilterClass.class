
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkMultiFilterClass
  var ptr: NullablePointer[GtkMultiFilterClassT]

  new create(ptr': NullablePointer[GtkMultiFilterClassT]) => ptr = ptr'
