
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeSortableIface
  var ptr: NullablePointer[GtkTreeSortableIfaceT]

  new create_from_ptr(ptr': NullablePointer[GtkTreeSortableIfaceT]) => ptr = ptr'
