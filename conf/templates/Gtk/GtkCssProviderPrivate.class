
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkCssProviderPrivate
  var ptr: NullablePointer[GtkCssProviderPrivateT]

  new create(ptr': NullablePointer[GtkCssProviderPrivateT]) => ptr = ptr'
