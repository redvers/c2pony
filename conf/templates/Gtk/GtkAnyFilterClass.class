
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkAnyFilterClass
  var ptr: NullablePointer[GtkAnyFilterClassT]

  new create(ptr': NullablePointer[GtkAnyFilterClassT]) => ptr = ptr'
