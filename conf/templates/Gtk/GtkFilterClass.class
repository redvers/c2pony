
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkFilterClass
  var ptr: NullablePointer[GtkFilterClassT]

  new create() =>
    ptr = NullablePointer[GtkFilterClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkFilterClassT]) => ptr = ptr'
