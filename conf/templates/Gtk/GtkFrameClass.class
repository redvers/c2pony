
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkFrameClass
  var ptr: NullablePointer[GtkFrameClassT]

  new create() =>
    ptr = NullablePointer[GtkFrameClassT].none()
  new create_from_ptr(ptr': NullablePointer[GtkFrameClassT]) => ptr = ptr'
