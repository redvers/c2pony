
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkActionableInterface
  var ptr: NullablePointer[GtkActionableInterfaceT]

  new create_from_ptr(ptr': NullablePointer[GtkActionableInterfaceT]) => ptr = ptr'
