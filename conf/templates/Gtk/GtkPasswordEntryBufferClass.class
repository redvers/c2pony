
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkPasswordEntryBufferClass
  var ptr: NullablePointer[GtkPasswordEntryBufferClassT]

  new create(ptr': NullablePointer[GtkPasswordEntryBufferClassT]) => ptr = ptr'
