
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkSorterClass
  var ptr: NullablePointer[GtkSorterClassT]

  new create_from_ptr(ptr': NullablePointer[GtkSorterClassT]) => ptr = ptr'
