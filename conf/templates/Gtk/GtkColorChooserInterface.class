
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkColorChooserInterface
  var ptr: NullablePointer[GtkColorChooserInterfaceT]

  new create(ptr': NullablePointer[GtkColorChooserInterfaceT]) => ptr = ptr'
