
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkBitset
  var ptr: NullablePointer[GtkBitsetT]

  new create() =>
    ptr = NullablePointer[GtkBitsetT].none()
  new create_from_ptr(ptr': NullablePointer[GtkBitsetT]) => ptr = ptr'
