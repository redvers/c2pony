
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeRowReference
  var ptr: NullablePointer[GtkTreeRowReferenceT]

  new create(ptr': NullablePointer[GtkTreeRowReferenceT]) => ptr = ptr'
