
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkTreeRowReference
  var ptr: NullablePointer[GtkTreeRowReferenceT]

  new create() =>
    ptr = NullablePointer[GtkTreeRowReferenceT].none()
  new create_from_ptr(ptr': NullablePointer[GtkTreeRowReferenceT]) => ptr = ptr'
