
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkBitsetIter
  var ptr: NullablePointer[GtkBitsetIterT]

  new create_from_ptr(ptr': NullablePointer[GtkBitsetIterT]) => ptr = ptr'
