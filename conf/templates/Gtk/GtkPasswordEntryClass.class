
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkPasswordEntryClass
  var ptr: NullablePointer[GtkPasswordEntryClassT]

  new create(ptr': NullablePointer[GtkPasswordEntryClassT]) => ptr = ptr'
