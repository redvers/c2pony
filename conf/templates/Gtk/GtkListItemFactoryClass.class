
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkListItemFactoryClass
  var ptr: NullablePointer[GtkListItemFactoryClassT]

  new create_from_ptr(ptr': NullablePointer[GtkListItemFactoryClassT]) => ptr = ptr'
