
use "../GLib"
use "../GObject"
use "../Gio"
use "lib:gtk-4"
    
class GtkListItemFactoryClass
  var ptr: NullablePointer[GtkListItemFactoryClassT]

  new create(ptr': NullablePointer[GtkListItemFactoryClassT]) => ptr = ptr'
