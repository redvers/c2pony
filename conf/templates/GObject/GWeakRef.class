
use "../GLib"
use "lib:gobject-2.0"

class GWeakRef
  var ptr: NullablePointer[GWeakRefT]

  new create() =>
    ptr = NullablePointer[GWeakRefT].none()
  new create_from_ptr(ptr': NullablePointer[GWeakRefT]) => ptr = ptr'
