
use "../GLib"
use "lib:gobject-2.0"

class GSignalQuery
  var ptr: NullablePointer[GSignalQueryT]

  new create() =>
    ptr = NullablePointer[GSignalQueryT].none()
  new create_from_ptr(ptr': NullablePointer[GSignalQueryT]) => ptr = ptr'
