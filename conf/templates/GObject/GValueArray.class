
use "../GLib"
use "lib:gobject-2.0"

class GValueArray
  var ptr: NullablePointer[GValueArrayT]

  new create() =>
    ptr = NullablePointer[GValueArrayT].none()
  new create_from_ptr(ptr': NullablePointer[GValueArrayT]) => ptr = ptr'
