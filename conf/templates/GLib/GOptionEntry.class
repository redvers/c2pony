
use "lib:glib-2.0"

class GOptionEntry
  var ptr: NullablePointer[GOptionEntryT]

  new create() =>
    ptr = NullablePointer[GOptionEntryT].none()
  new create_from_ptr(ptr': NullablePointer[GOptionEntryT]) => ptr = ptr'
