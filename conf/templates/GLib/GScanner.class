
use "lib:glib-2.0"

class GScanner
  var ptr: NullablePointer[GScannerT]

  new create() =>
    ptr = NullablePointer[GScannerT].none()
  new create_from_ptr(ptr': NullablePointer[GScannerT]) => ptr = ptr'
