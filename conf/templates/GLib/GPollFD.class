
use "lib:glib-2.0"

class GPollFD
  var ptr: NullablePointer[GPollFDT]

  new create() =>
    ptr = NullablePointer[GPollFDT].none()
  new create_from_ptr(ptr': NullablePointer[GPollFDT]) => ptr = ptr'
