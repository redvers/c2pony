
use "lib:glib-2.0"

class GIOChannel
  var ptr: NullablePointer[GIOChannelT]

  new create() =>
    ptr = NullablePointer[GIOChannelT].none()
  new create_from_ptr(ptr': NullablePointer[GIOChannelT]) => ptr = ptr'
