
use "lib:glib-2.0"

class GScannerConfig
  var ptr: NullablePointer[GScannerConfigT]

  new create() =>
    ptr = NullablePointer[GScannerConfigT].none()
  new create_from_ptr(ptr': NullablePointer[GScannerConfigT]) => ptr = ptr'
