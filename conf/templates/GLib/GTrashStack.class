
use "lib:glib-2.0"

class GTrashStack
  var ptr: NullablePointer[GTrashStackT]

  new create() =>
    ptr = NullablePointer[GTrashStackT].none()
  new create_from_ptr(ptr': NullablePointer[GTrashStackT]) => ptr = ptr'
