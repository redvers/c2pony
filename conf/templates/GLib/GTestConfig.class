
use "lib:glib-2.0"

class GTestConfig
  var ptr: NullablePointer[GTestConfigT]

  new create() =>
    ptr = NullablePointer[GTestConfigT].none()
  new create_from_ptr(ptr': NullablePointer[GTestConfigT]) => ptr = ptr'
